<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title.title %> - Echo Lab</title>
</head>
<body>
  <h1><%= title.title %></h1>
  <p><%= title.excerpt || 'No exceprt provided.' %></p>

  <h2>Figures</h2>
  <ul>
    <% figures.forEach(f => { %>
      <li><a href="/figures/<%= f.id %>"><%= f.name %></a></li>
    <% }) %>
  </ul>

  <h2>Concepts</h2>
  <ul>
    <% concepts.forEach(c => { %>
      <li><a href="/concepts/<%= c.id %>"><%= c.term %></a></li>
    <% }) %>
  </ul>

  <p><a href="/">Back to homepage</a></p>
</body>
</html>
